# AI Agent 从入门到精通（Python 手写）

> 版本：2.0
> 目标读者：AI Agent 初学者，能写基础 Python，希望系统掌握 Agent 并在工作中落地
> 学习方式：边做边学，代码随章节渐进创建；Claude Code 作为结对导师
> 设计原则：渐进式、主动参与、从底层原理到生产实践

---

## 0. 如何使用本文档（先读）

本文档是一份**课程设计 + 学习路线**，不是已经写好的完整代码库。学习方式是"边做边学"：

- **每个章节有 `TODO(人工)` 协作点**：遇到这个标记，停下来自己动手实现，而不是让 Claude 直接代做。讲解"思路"而非"答案"。
- **教学遵循 CGCC 框架**，每节课按 `Context → Goal → Constraints → Criteria` 组织（见下）。
- **先结论，再展开**：每个知识点先一句话讲核心，再拆开。
- **每节 ≤3 个关键块**，抽象概念配一个生活化类比。
- **代码随学随建**：目录结构（`src/...`）在对应阶段才出现，动手前先以实际文件状态为准。

### CGCC 框架

教学任务统一按这个结构组织：

- **Context（上下文）**：当前进度、已掌握概念、本次前置知识
- **Goal（目标）**：本节结束后能做什么（可验证的能力）
- **Constraints（约束）**：不引入尚未讲授的概念、代码 ≤50 行（练习除外）
- **Criteria（成功标准）**：能复述概念、能做题、能写实现

### 学习路线图

```
预备：心智模型 ──▶ 模块1 Prompt ──▶ 模块2 工具调用 ──▶ 模块3 上下文
                                                          │
       模块5 生产化 ◀────── 模块4 多智能体/范式 ◀──────────┘
```

贯穿全程的四个实战项目（P1→P4），难度随模块递进，最终汇成模块 5 的生产化 Agent。

---

## 一、心智模型：Agent 是什么

**一句话结论**：AI Agent = **LLM + 工具 + 循环 + 记忆**。把一个"只会聊天"的大模型，加上能做事的手（工具）、能自己决定下一步的大脑（循环）、和跨会话记住的东西（记忆），就成了 Agent。

### 1.1 Agent 与 Chatbot / Workflow / RAG 的边界

| 类型 | 是否调用工具 | 是否自己决定下一步 | 典型例子 |
|------|------|------|------|
| Chatbot（聊天机器人） | 否 | 否，一问一答 | 客服闲聊 |
| Workflow（工作流） | 是 | 否，步骤由代码写死 | 固定流程的数据 ETL |
| RAG（检索增强） | 部分 | 否，检索→生成两步 | 知识库问答 |
| **Agent** | **是** | **是，循环直到目标达成** | 能规划并执行的助手 |

**生活化类比**：Chatbot 是"只会答话的接线员"，Workflow 是"流水线上的机械臂"，Agent 是"能自己打电话、查资料、跟多个部门协调、最后给你结论的助理"。

### 1.2 Agent 的四个核心部件

1. **LLM（大脑）**：理解意图、推理、生成。
2. **工具（手）**：调用外部能力——查天气、搜网页、读文件、执行代码。
3. **循环（控制流）**：`调用模型 → 模型决定用哪个工具 → 执行工具 → 把结果喂回模型`，直到任务完成。这就是本课程模块 2 要手写的东西。
4. **记忆（长期记忆）**：短期（当前会话）、长期（跨会话）、情景（关键事件），模块 3 展开。

### 1.3 能力成熟度 L0–L4（贯穿全课的坐标系）

这四层是判断"一个 Agent 到底强不强"的尺子，全课程就在带着你从 L0 爬到 L4：

- **L0 无 Agent**：单次 LLM 调用，一问一答。
- **L1 单 Agent + 循环**：模型能调用工具，循环直到完成。（模块 2 达到）
- **L2 规划 + 反思**：能拆任务、自我批评、纠正错误。（模块 4 达到）
- **L3 上下文工程**：有记忆、能用 RAG 拉外部知识。（模块 3 达到）
- **L4 生产级 Harness**：有评估、可观测、有护栏、能部署。（模块 5 达到）

> 注意：L3 和 L4 在能力上是正交的——一个有记忆的 Agent（L3）未必能上生产（L4）。所以模块 3、4、5 不是严格的先后，而是三个维度。

---

## 二、预备：建立心智模型（第 1 周）

**目标**：能说清 Agent 是什么、跑通第一个 LLM 调用、搭好 Python 环境。

### 2.1 LLM 基础速通（15 分钟）

- **Token（令牌）**：模型读写的"最小单位"，一个汉字约 1–2 个 token，一个英文单词约 1–3 个。**按 token 计费**，这是后面所有成本优化的基础。
- **三个消息角色**：`system`（系统设定，规定行为）、`user`（用户输入）、`assistant`（模型回复）。多轮对话就是把历史消息每次全量重发——**API 是无状态的**。
- **关键参数**：`max_tokens`（本次回复上限）、`temperature`（随机性，越低越稳定）、`tools`（可用工具）、`stop_reason`（模型为什么停：正常结束 / 要调工具 / 撞上限）。

### 2.2 环境搭建 + 第一个调用

```bash
pip install anthropic
```

```python
import anthropic

client = anthropic.Anthropic()  # 自动读 ANTHROPIC_API_KEY 环境变量
MODEL = "claude-opus-5"

resp = client.messages.create(
    model=MODEL,
    max_tokens=1024,
    messages=[{"role": "user", "content": "用一句话解释什么是 token"}],
)
print(resp.content[0].text)
```

> **运行前**：在终端 `export ANTHROPIC_API_KEY=sk-...`（或 `ant auth login`）。不要在代码里硬编码密钥。

**`TODO(人工)`**：把上面的脚本跑通，然后改 `max_tokens=32` 再跑一次，观察输出被截断时 `resp.stop_reason` 变成什么值。这个现象正是模块 5"成本与护栏"要解决的问题。

### 2.3 环境变量与结构约定

从第一行代码就养成习惯：

- 密钥走环境变量，不进代码、不进 git。
- 目录约定（随课程推进才逐个创建）：`src/` 下按模块分目录。

---

## 三、模块 1：Prompt Engineering（第 2–3 周）

**一句话目标**：让同一个模型，通过"话术设计"产出更稳定、更可解析的结果。这是所有 Agent 的地基——Agent 的每一步，本质上都在给模型写 prompt。

### 3.1 结构化 Prompt

**Goal**：设计一个含 Role / Task / Constraints / Output Format 四部分的提示词。

- **为什么要结构化**：模型输出质量对措辞极敏感。把"你是谁、要干嘛、有什么限制、输出什么格式"分开写，可复现、可维护。
- **关键块**：`Role`（角色）、`Task`（任务）、`Constraints`（约束）、`Output Format`（输出格式，最好要求 JSON）。
- **变量注入**：固定文本 + 动态变量。用 Python f-string 或 Jinja2，并为每个变量给默认值（fallback）。

```python
TEMPLATE = """你是{role}。
任务：{task}
约束：{constraints}
输出：只返回 JSON，形如 {{"result": "..."}}
"""

def build_prompt(**kw):
    defaults = {"role": "代码审查员", "constraints": "只针对逻辑，不挑剔风格"}
    return TEMPLATE.format(**(defaults | kw))
```

**`TODO(人工)`**：写一个"代码审查"的结构化 prompt，要求模型把问题按 `{"严重": [...], "建议": [...]}` 输出。评测它连续 10 次输出是否都是合法 JSON。

### 3.2 Few-shot 与 CoT

**Goal**：用示例和推理步骤把"难任务"变成"稳定任务"。

- **Few-shot（少样本）**：给 1–3 个"输入→正确输出"的例子。示例要覆盖**简单、中等、难**三个难度，比给一堆相似例子有效。
- **CoT（思维链）**：要求"先逐步思考再给结论"。用 `Step 1 / Step 2` 格式，能显著提升多步推理正确率。
- **类比**：Few-shot 是"给实习生看两个范文"，CoT 是"要求他把草稿纸演算过程也交上来"。

**`TODO(人工)`**：设计一个含 3 个示例的 few-shot 提示词，对比加/不加 CoT 时，同一道推理题的答案差异。

### 3.3 输出约束与结构化输出

**Goal**：让模型返回**能被代码直接消费**的结构化数据，而不是"读起来像 JSON 的文本"。

**结论**：不要靠"请你返回 JSON"这种提示词赌博，要用**结构化输出 API** 硬约束格式。

```python
from pydantic import BaseModel

class Review(BaseModel):
    severity: str          # "blocking" | "suggestion"
    file: str
    issue: str

resp = client.messages.parse(
    model=MODEL, max_tokens=2048,
    messages=[{"role": "user", "content": "审查这段代码: ..."}],
    output_format=Review,          # 自动按 schema 校验
)
review: Review = resp.parsed_output  # 已是类型安全的 Review 对象
```

- `messages.parse` 返回的 `parsed_output` 已经是校验过的对象——字段缺了、类型错了会直接报错。
- 底层等价写法：`messages.create(..., output_config={"format": {"type": "json_schema", "schema": {...}}})`。
- **fallback 策略**：字段缺失时怎么办？在 schema 里给默认值，或捕获校验异常走"重试一次"。

### 3.4 Prompt 评测

**Goal**：能回答"我的 prompt 到底改好了没有"。

- **做法**：先造一个 10–20 题的评测集（每题标注期望输出），改 prompt 前后各跑一遍，比较**一致率**和**正确率**。这是模块 5"评估体系"的雏形。
- **金句**：没有评测集的 prompt 调优，等于闭眼开车。

**项目连接**：这四周产出的 prompt 模板，会成为 P1「文档问答 Agent」里"抽取"和"检索"环节的零件。

---

## 四、模块 2：工具调用 Function Calling（第 4–5 周）

**一句话目标**：学会写 Agent 的核心——**agentic loop**（智能体循环）。这是 Agent 区别于 chatbot 的根本，也是本课程的分水岭。

### 4.1 为什么 LLM 需要工具

模型的知识有时间截止、不能执行、不能查实时数据。工具给它"手"：

- 模型不"知道"结果，它只输出**"我要调用哪个工具、传什么参数"**这个意图。
- 你的代码负责**真正执行**工具，再把结果喂回去。
- **类比**：模型是大脑，只会说"帮我查一下北京天气"；工具调用就是那个"递电话的手"，大脑说了号码，手去拨。

### 4.2 工具 schema 设计

**Goal**：写好 `name / description / input_schema`，让模型"知道怎么用"。

```python
tools = [
    {
        "name": "get_weather",
        "description": "查询指定城市的当前天气。",
        "input_schema": {
            "type": "object",
            "properties": {
                "location": {"type": "string", "description": "城市名，如「北京」"},
                "unit": {"type": "string", "enum": ["celsius", "fahrenheit"]},
            },
            "required": ["location"],
        },
    }
]
```

- **description 是写给模型看的"使用说明"**：说清"什么场景用、参数什么意思、返回什么"。描述写差了，模型就用错。
- **input_schema 是参数契约**：类型、枚举、必填。可选时用 `strict: True` 强制精确校验。

### 4.3 手写 Agent 循环（本模块核心）

**Goal**：从零写出 `调用 → 决定工具 → 执行 → 喂回 → 直到完成` 的循环。

```python
import anthropic

client = anthropic.Anthropic()
MODEL = "claude-opus-5"

def execute_tool(name: str, args: dict) -> str:
    if name == "get_weather":
        return f"{args['location']} 现在 26°C，晴"   # 真实项目中这里调真实 API
    raise ValueError(f"未知工具: {name}")

def run_agent(user_input: str) -> str:
    messages = [{"role": "user", "content": user_input}]
    while True:
        resp = client.messages.create(
            model=MODEL, max_tokens=4096, tools=tools, messages=messages,
        )
        if resp.stop_reason == "end_turn":          # 模型不调工具了，任务结束
            return next(b.text for b in resp.content if b.type == "text")

        messages.append({"role": "assistant", "content": resp.content})

        results = []                                # 收集所有 tool_result
        for block in resp.content:
            if block.type == "tool_use":
                out = execute_tool(block.name, block.input)
                results.append({
                    "type": "tool_result",
                    "tool_use_id": block.id,        # 必须匹配，否则模型对不上
                    "content": out,
                })
        messages.append({"role": "user", "content": results})
```

**逐行理解（3 个关键块）**：

1. **`stop_reason` 是循环的开关**：`tool_use` = 模型想用工具（继续循环）；`end_turn` = 完成（退出）。
2. **`tool_use` 块**里藏着 `name`（要哪个工具）和 `input`（参数），你的 `execute_tool` 据此执行。
3. **`tool_result` 必须带对的 `tool_use_id`** 塞回消息列表——这是"把工具结果告诉模型"的唯一方式。

**`TODO(人工)`**：给 `execute_tool` 再加一个工具（比如 `search_web(query)` 返回一条模拟结果），让 Agent 能"先查天气、再查航班"连续调用两次，观察循环跑了几圈。

### 4.4 工具错误处理

**Goal**：工具失败了 Agent 不能崩，要能自己纠正。

```python
# 工具失败时，返回 is_error=True，而不是 throw
results.append({
    "type": "tool_result",
    "tool_use_id": block.id,
    "content": "未找到该城市，请提供有效城市名",
    "is_error": True,
})
```

- **`is_error: True`** 告诉模型"这次调用失败"，模型会据此解释或换个参数重试。
- **兜底**：还要防"死循环"——给循环加最大轮数上限（如 20 轮），超了强制结束并报告。
- **解析安全**：永远用 `json.loads()` 解析工具入参，不要用字符串匹配。

**项目连接**：本模块的 `run_agent` 就是 P2「自动化工作流 Agent」的引擎，后面所有 Agent 都复用这一套循环。

---

## 五、模块 3：Context Engineering（第 6–7 周）

**一句话目标**：让 Agent 会"找资料"（RAG）和"记住事"（记忆），并管理好有限的上下文。

### 5.1 上下文窗口与 token 预算

- **模型上下文有上限**（如 1M token），超了要么截断要么报错。Agent 循环每一轮都在累积历史，**很快就会用光**。
- **管理手段**：精简历史、摘要压缩（把早期对话压缩成一段摘要）、缓存复用的稳定前缀。
- **类比**：上下文窗口是"工作台大小"，放不下所有文件；你得决定"哪些放台上、哪些收进抽屉（记忆/检索）"。

### 5.2 RAG 全栈

**Goal**：搭建一个能回答"私有知识库"问题的文档问答 Agent。

核心四步：

1. **分块（Chunking）**：把长文档切成片段。**按语义切**（章节/段落），而非固定长度硬切。
2. **嵌入（Embedding）**：把文本转成向量，建索引。
3. **检索（Retrieval）**：把问题也转成向量，找最相近的片段（相似度排序）。进阶用 **HyDE**（先用模型"假设一份答案"再去检索）和**多路召回**（关键词 + 向量 + 结构化混合）。
4. **生成（Generation）**：把检索到的片段作为上下文，让模型作答，并要求"找不到就说不知道"（防幻觉）。

**`TODO(人工)`**：实现检索函数的多路召回——同时用「关键词命中」和「向量相似度」两条路取结果，合并去重后重排序。

### 5.3 记忆系统

**Goal**：让 Agent 跨会话记住用户偏好和历史决策。

三层记忆：

- **短期记忆**：当前会话的消息列表（就存在内存里）。
- **长期记忆**：跨会话，存 SQLite；每条含**时间戳 + 重要性评分**，检索时按向量相似度取相关条目。
- **情景记忆**：关键事件摘要，存向量库。

**`TODO(人工)`**：实现"重要性评分"算法（例如：用户显式说"记住"的记高分，普通闲聊记低分）；写一个"清理/压缩"函数，把低分且过期的条目删掉。

### 5.4 MCP（Model Context Protocol）

**Goal**：理解 MCP 是什么、解决什么问题。

- **一句话**：MCP 是"工具/数据源的标准接口"，让任何 Agent 用统一方式连接外部工具和数据，而不需要为每个工具各写一套适配。
- **对比**：没有 MCP 时，每接一个系统就写一段螺丝刀；有了 MCP，大家都用"标准插座"。
- **实践**：Python 里用 `anthropic[mcp]` 把 MCP server 暴露的工具转成可用工具，或直接用 SDK 的 `mcp_servers` 参数直连。

**项目连接**：P1「文档问答 Agent」在模块 3 完成——RAG 负责"找资料"，记忆负责"记住用户问过什么"。

---

## 六、模块 4：Agent Engineering（第 8–10 周）

**一句话目标**：让 Agent 会"规划、反思、协作"，从"会调工具"进化到"能搞定复杂任务"。

### 6.1 Agent 范式演进

四个范式，从简单到强（每个都是 prompt 层面的设计 + 循环控制）：

| 范式 | 一句话 | 何时用 |
|------|------|------|
| **ReAct** | 边想边做：交替输出"思考 + 行动" | 单步工具调用，最通用（模块 2 学的就是它的骨架） |
| **Plan-and-Execute** | 先列完整计划，再逐步执行 | 任务可预先拆解、步骤较固定 |
| **ReWOO** | 把计划与执行**解耦**，先收集全部信息再一次性作答 | 想省 token，减少来回 |
| **Reflexion** | 执行后自我批评，带"失败教训"重试 | 需要迭代改进的难任务 |

**`TODO(人工)`**：把手里的 ReAct Agent 改造成 Plan-and-Execute——加一个"先输出 plan，再逐个执行"的循环，观察失败率下降多少。

### 6.2 规划与反思

- **规划**：复杂任务先拆子任务，再逐个击破。关键：**每步可验证**。
- **反思（Critic）**：让模型在执行后问自己"结果对不对？下一步该不该调整？"，输出 `通过 / 需修改 / 重试` 的判断。
- **自愈循环**：`执行 → 检查 → 不达标就重试（带上错误原因）`，是 Harness 法则之一（"强约束 + 自愈循环"）。

### 6.3 多智能体协作

**Goal**：用多个各司其职的 Agent 协作，而非一个 Agent 硬扛。

经典模式：

- **Planner → Executor → Critic**：规划、执行、审查分离，各跑在独立上下文里（互不干扰）。
- **Orchestrator → Worker**：一个调度者把子任务分发给多个"工人"，收回来汇总。
- **辩论 / 多视角**：让两个 Agent 各自主张、互相挑错，取更稳的结论。

**判断什么时候该上多智能体**：任务真的能拆成并行、或需要独立审查时再用。简单任务上多智能体，只是烧钱。

**`TODO(人工)`**：把你手写的 `run_agent` 改造成 Planner/Executor 两个函数，分别用不同的 system prompt，看"规划"是否能提升一个三步任务的完成率。

### 6.4 工具系统与安全

**Goal**：工具多了、危险了，怎么保安全。

- **白名单机制**：允许调用的工具/操作列成清单（如 `whitelist.json`），清单外的调用一律拒绝。
- **危险操作拦截**：删除文件、发网络请求、执行命令——需要确认或审计日志。
- **工具描述必须写清**：`名称 / 参数 / 返回值 / 使用场景 / 安全限制`。
- **原则**：永远假设模型可能被 prompt 注入操纵，把"能做的动作"收紧到最小。

**项目连接**：P2「自动化工作流 Agent」在此完成——用多步骤工具编排完成一个真实任务（如抓数据→处理→写报告）。

---

## 七、模块 5：Harness 生产化（第 11–14 周）

**一句话目标**：把"能跑的 Agent"变成"敢上线、能监控、出错能兜底"的产品。这是"工作中应用"和"玩具"的分水岭。

### 7.1 评估体系（Evals）

**Goal**：能量化"我的 Agent 到底好不好"。

- **三层评估**：
  - 单步评估：单个工具调用/输出对不对；
  - 轨迹评估：整个多步过程是否合理（有无多余/错误步骤）；
  - 端到端评估：最终结果是否达成目标。
- **做法**：造含正常/边界/异常三类输入的评测集，每次改完跑一遍做**回归**。这是 3.4 的系统化。

### 7.2 可观测性

**Goal**：Agent 在生产里"黑盒"了，要能看见里面发生了什么。

- **日志（logging）**：每一轮 `model → tool → result` 记下来。
- **追踪（tracing）**：一个任务的多步调用串成一条链路，定位卡在哪一步。
- **指标（metrics）**：成功率、平均轮数、token 消耗、延迟分布。
- 代码里至少做到：给 `run_agent` 加结构化日志，记录每轮用了什么工具、耗时多久。

### 7.3 可靠性与护栏

**Goal**：出错不崩、能兜底。

- **重试与退避**：限流/5xx 用指数退避重试（SDK `max_retries` 已内置部分）。
- **降级**：主模型挂了切备选模型；工具超时给默认值。
- **护栏（guardrails）**：输入进来先过一道检查（注入、敏感、越权），输出出去再过一道检查（有害、格式）。
- **死循环防护**：最大轮数 + 预算上限（`task_budget` 之类），超了强制收尾。

### 7.4 成本与性能

**Goal**：同样的能力，花更少的钱和时间。

- **模型路由**：简单任务用便宜模型（如 haiku），难的才上 Opus。
- **Prompt 缓存**：把稳定前缀（system prompt、工具列表）缓存，重复请求便宜一个量级。
- **减速不必要输出**：限制 `max_tokens`、去掉多余 thinking，减少来回轮数。
- **先量后剪**：用 7.2 的 metrics 找出"烧钱大户"是哪个环节再优化。

### 7.5 部署与运维

**Goal**：从"本机脚本"到"能给人用的服务"。

- 把 `run_agent` 包成一个服务端点（如 FastAPI），配环境变量、健康检查。
- 敏感配置（密钥）走环境/密钥管理，绝不进仓库。
- 本地脚本 → 服务化 → CI 里跑 Evals 回归，逐步演进。

### 7.6 安全与治理

**Goal**：防注入、防越权、可有审计。

- **Prompt 注入**：用户输入里夹带"忽略之前的指令"。对策：把用户内容与系统指令隔离、校验工具调用参数来源。
- **越权**：Agent 能碰到的资源要按用户权限最小化，不能因为"模型要求了"就执行敏感操作。
- **审计**：危险操作留痕，可追溯。

**最终交付**：P4——一个带 Evals + 日志 + 护栏 + 部署配置的生产级 Agent。

---

## 八、贯穿实战项目

四个项目，越往后越完整，**每完成一个都要写进 `progress/` 的进度报告**：

| 项目 | 对应模块 | 交付标准 |
|------|------|------|
| **P1 文档问答 Agent** | 模块 3 | 能对私有知识库问答，答不出时明确说"不知道" |
| **P2 自动化工作流 Agent** | 模块 4 | 用 ≥3 个工具编排完成一个多步任务，带日志 |
| **P3 客服工单 Agent** | 模块 3+4 | 多智能体协作 + 记忆，能记住用户上下文 |
| **P4 生产级 Agent** | 模块 5 | 带 Evals、可观测、护栏、部署配置，通过回归测试 |

---

## 九、进度追踪

每完成一个模块，更新 `progress/ai-agent-progress.md`：

```markdown
# 学习进度报告

## 总体进度
- 已完成：6 / 16 章节
- 当前模块：模块 2 工具调用

## 各章节掌握度
| 章节 | 掌握度 | 状态 |
|------|--------|------|
| 2.3 手写 Agent 循环 | 85% | ✅ 已通过 |
| 4.1 Agent 范式 | 60% | 🔄 需复习 |

## 薄弱环节
- 工具 schema 设计（description 写得太含糊）

## 建议复习
- 4.1 Agent 范式：3 天后
```

---

## 十、设计原则速查表

| 原则 | 说明 | 落地 |
|------|------|------|
| 从底层原理到生产 | 先手写循环，再谈框架 | 模块 2 手写，模块 5 才谈部署 |
| 主动学习 | 学习者参与设计 | `TODO(人工)` 协作点 |
| 即时评估 | 每节验证掌握度 | 每节练习 + Evals |
| 渐进复杂度 | 一次只跨一步 | L0→L4 递进 |
| 生产思维 | 能跑 ≠ 能上线 | 评估/可观测/护栏 |

---

> **使用说明**
> 1. 环境：`pip install anthropic`，密钥走 `ANTHROPIC_API_KEY` 环境变量。
> 2. 学习节奏：按周推进，每个模块结束跑一次对应实战项目。
> 3. 代码约定：目录 `src/` 按模块创建，动手前先看实际文件状态。
> 4. 遇到 `TODO(人工)` 先自己写，再对照讲解。